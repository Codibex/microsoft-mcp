# v0.10.0 - Graph integration fixes

## Highlights

- Fixes Teams Microsoft Graph request paths and updates the related policy documentation
- Resolves nested Outlook mail folders recursively for folder-scoped operations
- Hardens Graph integration error handling and response mapping across the servers
- Adds regression coverage for Teams, Outlook folder resolution and Graph error handling

## Upgrade notes

- No changes are required to existing `TenantId`/`ClientId` configuration or permissions.
- Existing policy files remain compatible with the current policy format.

Full changelog: https://github.com/Codibex/microsoft-mcp/compare/v0.9.0...v0.10.0